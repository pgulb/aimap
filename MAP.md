# aimap — Architecture Map

## Overview

`aimap` is a CLI tool written in Go that scans Go and Python projects and generates `MAP.md` — a symbol map of functions, objects, methods, variables, and their line ranges. It is designed to help AI agents (and human contributors) locate code symbols quickly without full-repository searches.

The architecture is a pipeline: CLI entry → config loading → file tree scan → source parsing → symbol output.

**This document has two sections:**

1. **Architectural overview** (above) — manually maintained. Describes the project structure, packages, key types, and cross-module dependencies.
2. **Symbol map** (below) — **automatically generated** by running `aimap` in the project root. Lists every function, method, type, variable, and constant with file paths and line numbers.

To regenerate the symbol map section:

```bash
go run ./cmd/aimap/
```

## Project Structure

```
aimap/
├── go.mod                  # Go module definition (module aimap, go 1.26.5)
├── AGENTS.md               # Agent guidance
├── MAP.md                  # This file — architectural index
├── SECURITY.md             # Security guidelines
├── BEST_PRACTICES.md       # Development conventions
├── TESTING.md              # Testing guide
├── cmd/
│   └── aimap/
│       └── main.go         # CLI entry point — flag parsing, pipeline wiring
├── internal/
│   ├── symbol/
│   │   ├── doc.go          # Package documentation
│   │   └── symbol.go       # Symbol struct and SymbolKind enum
│   ├── ignore/
│   │   ├── doc.go          # Package documentation
│   │   ├── ignore.go       # Matcher — built-in + .aimapignore pattern matching
│   │   └── ignore_test.go  # Tests
│   ├── scanner/
│   │   ├── doc.go          # Package documentation
│   │   ├── scanner.go      # Scanner — WalkDir, file classification, ignore integration
│   │   └── scanner_test.go # Tests
│   ├── config/
│   │   ├── doc.go          # Package documentation
│   │   ├── config.go       # Load — reads .aimapignore, resolves root path
│   │   └── config_test.go  # Tests
│   ├── parser/
│   │   ├── doc.go          # Package documentation
│   │   ├── parser_go.go    # Go parser using go/ast + go/parser (stdlib)
│   │   ├── parser_python.go# Python parser (line-based state machine)
│   │   ├── parser_test.go  # Tests
│   │   └── testdata/
│   │       └── src/        # Fixture files for parser tests
│   │           ├── hello.go
│   │           ├── comments.go
│   │           └── empty.go
│   ├── output/
│   │   ├── doc.go          # Package documentation
│   │   ├── output.go       # Render — markdown generator grouped by language/file
│   │   └── output_test.go  # Tests
│   └── symbol/             # (repeated in tree above)
└── .aimapignore            # User-defined ignore patterns (optional)
```

## Packages

| Package | Files | Responsibility |
|---|---|---|
| `cmd/aimap/main.go` | 1 | CLI entry, flag parsing, pipeline wiring |
| `internal/symbol/` | 2 | `Symbol` struct (Name, Kind, FilePath, LineStart, LineEnd, Comment, Parent), `SymbolKind` enum (Function, Method, Struct, Interface, Type, Variable, Constant, Class) |
| `internal/ignore/` | 2 + tests | `Matcher` with built-in ignores + `.aimapignore` gitignore-style parsing |
| `internal/scanner/` | 1 + tests | `Scanner` walking directory tree via `filepath.WalkDir`, classifying `.go`/`.py`/other |
| `internal/config/` | 1 + tests | `Config.Load()` — resolves root, loads `.aimapignore`, returns `*Config` with `*Matcher` |
| `internal/parser/` | 2 + tests | Go parser via `go/ast`; Python parser via line-based state machine |
| `internal/output/` | 1 + tests | Render symbols to markdown grouped by language section then file path |

## Key Design Decisions

- **No external dependencies** — uses Go standard library exclusively
- **Single-binary CLI** — no config file required (optional `.aimapignore`)
- **Output to `MAP.md`** — overwrites symbol listing below the SYMBOL MAP marker, preserves architectural overview above it

<!-- GENERATED BY aimap. The symbol map section below is auto-generated. Do not edit manually. -->

<!-- SYMBOL MAP -->

## Symbol Map

### Go Files

#### cmd/aimap/main.go

- `main` — function (lines 15-90)

#### internal/config/config.go

- `ignoreHeader` — constant (line 11)
- `ignoreHeaderDev` — constant (line 13)
- `Config` — struct (lines 16-19) — Config holds the runtime configuration for aimap.
- `Load` — function (lines 23-39) — Load reads configuration from the project root directory.
- `EnsureIgnoreFile` — function (lines 43-65) — EnsureIgnoreFile creates the given ignore file in root if it does not exist.
- `hasHeader` — function (lines 69-78) — hasHeader checks if the first non-empty, non-whitespace line starts with the

#### internal/config/config_test.go

- `TestLoadNoIgnoreFile` — function (lines 12-28)
- `TestLoadWithIgnoreFile` — function (lines 30-49)
- `TestLoadResolvesAbsPath` — function (lines 51-59)
- `TestMatcherIsIgnoreMatcher` — function (lines 61-71)
- `TestEnsureIgnoreFileCreatesIfMissing` — function (lines 73-89)
- `TestEnsureIgnoreFileDev` — function (lines 91-107)
- `TestEnsureIgnoreFilePreservesExisting` — function (lines 109-130)
- `TestEnsureIgnoreFilePrependsHeaderIfMissing` — function (lines 132-156)
- `TestEnsureIgnoreFileNoopOnEmptyHeader` — function (lines 158-179)

#### internal/ignore/ignore.go

- `builtinPatterns` — variable (lines 11-31)
- `Matcher` — struct (lines 34-36) — Matcher checks whether a given path should be ignored.
- `NewMatcher` — function (lines 40-45) — NewMatcher creates a Matcher with the built-in ignore patterns combined with
- `LoadMatcherFromFile` — function (lines 50-65) — LoadMatcherFromFile reads ignore patterns from path (one per line, gitignore-style)
- `parsePatterns` — function (lines 67-78)
- `Matcher.Ignored` — method (lines 82-100) — Ignored checks whether path should be skipped. path is relative to the project root.
- `matchPath` — function (lines 104-131) — matchPath checks if a single pattern matches the given path.

#### internal/ignore/ignore_test.go

- `TestBuiltinPatterns` — function (lines 8-32)
- `TestCustomPatterns` — function (lines 34-53)
- `TestParsePatterns` — function (lines 55-71)
- `TestLoadMatcherFromFileMissing` — function (lines 73-84)

#### internal/output/output.go

- `marker` — constant (line 13)
- `generatedNotice` — constant (line 15)
- `kindLabel` — function (lines 17-38)
- `Render` — function (lines 45-145) — Render writes symbols to the specified output path as a markdown file.
- `relativize` — function (lines 147-153)
- `formatLineRange` — function (lines 155-160)

#### internal/output/output_test.go

- `TestRenderEmpty` — function (lines 12-36)
- `TestRenderPreservesExistingOverview` — function (lines 38-74)
- `TestRenderPathsAreRelative` — function (lines 76-109)
- `TestRenderGoSymbols` — function (lines 111-154)
- `TestRenderPythonSymbols` — function (lines 156-192)
- `TestRenderLineRanges` — function (lines 194-220)
- `TestKindLabel` — function (lines 222-243)

#### internal/parser/parser_go.go

- `ParseGoFile` — function (lines 13-35) — ParseGoFile parses a single Go source file and returns the symbols found in it.
- `parseGenDecl` — function (lines 38-91) — parseGenDecl handles var, const, type, and import declarations.
- `parseFuncDecl` — function (lines 94-113) — parseFuncDecl handles function and method declarations.
- `receiverName` — function (lines 116-126) — receiverName extracts the type name from a receiver expression.
- `docCommentFromGroup` — function (lines 128-133)
- `ParseGoDir` — function (lines 137-155) — ParseGoDir parses all .go files in a directory.
- `parseASTFile` — function (lines 157-170)
- `ParseFile` — function (lines 173-188) — ParseFile detects the language from the file extension and parses accordingly.
- `fileExt` — function (lines 190-200)

#### internal/parser/parser_python.go

- `ParsePythonFile` — function (lines 12-120) — ParsePythonFile parses a Python source string and extracts symbols.
- `collectComment` — function (lines 123-129) — collectComment accumulates lines into a pending doc comment.
- `extractPythonName` — function (lines 132-149) — extractPythonName extracts the name after a keyword (def or class).
- `extractAssignName` — function (lines 152-173) — extractAssignName gets the variable name from a top-level assignment.
- `findFunctionEnd` — function (lines 176-193) — findFunctionEnd finds the last line of a function body given the starting line (1-based) and indentation.
- `leadingSpaces` — function (lines 196-208) — leadingSpaces counts the number of leading spaces in a line.
- `isTopLevel` — function (lines 211-213) — isTopLevel checks if indentation indicates top-level scope.

#### internal/parser/parser_test.go

- `TestParseGoFile` — function (lines 10-71)
- `TestParseGoFileWithComments` — function (lines 73-92)
- `TestParseEmptyGoFile` — function (lines 94-102)
- `TestParseNonExistentGoFile` — function (lines 104-109)
- `TestParsePythonFile` — function (lines 111-190)
- `TestParsePythonEmptyFile` — function (lines 192-200)
- `TestParsePythonLineNumbers` — function (lines 202-236)
- `TestParseGoVariable` — function (lines 238-257)

#### internal/parser/testdata/src/comments.go

- `DocFunc` — function (lines 6-8) — DocFunc is a function with a doc comment.
- `UndocFunc` — function (lines 11-12) — UndocFunc has no doc comment body.

#### internal/parser/testdata/src/hello.go

- `Greeting` — function (lines 7-9) — Greeting returns a friendly message.
- `Config` — struct (lines 12-15) — Config holds application configuration.
- `Formatter` — interface (lines 18-20) — Formatter defines output formatting.
- `Config.GetName` — method (lines 23-25) — GetName returns the config name.
- `AppVersion` — constant (line 28) — AppVersion is the current version.
- `DebugVar` — variable (line 31) — DebugVar controls debug output.

#### internal/scanner/scanner.go

- `Language` — type (line 11) — Language identifies the programming language of a source file.
- `LanguageGo` — constant (line 14)
- `LanguagePython` — constant (line 15)
- `LanguageOther` — constant (line 16)
- `FileInfo` — struct (lines 20-23) — FileInfo describes a source file to be parsed.
- `Scanner` — struct (lines 26-28) — Scanner walks a directory tree and returns files to parse.
- `NewScanner` — function (lines 31-33) — NewScanner creates a Scanner that uses the given matcher for ignore rules.
- `Scanner.Scan` — method (lines 36-80) — Scan walks root and returns all non-ignored source files.
- `classifyFile` — function (lines 82-92)

#### internal/scanner/scanner_test.go

- `TestClassifyFile` — function (lines 11-31)
- `TestScan` — function (lines 33-90)

#### internal/symbol/symbol.go

- `SymbolKind` — type (line 4) — SymbolKind identifies the kind of a declared symbol.
- `Function` — constant (line 7)
- `Method` — constant (line 8)
- `Struct` — constant (line 9)
- `Interface` — constant (line 10)
- `Type` — constant (line 11)
- `Variable` — constant (line 12)
- `Constant` — constant (line 13)
- `Class` — constant (line 14)
- `Symbol` — struct (lines 18-26) — Symbol represents a named declaration in source code.

